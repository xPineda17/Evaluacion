let sueldos = [];
let empleados = parseInt(prompt('Ingrese la cantidad de empleados: '))

for(x=1; x<=empleados; x++){
    let empleado = prompt('Nombre empleado: ')
    let sueldo = parseInt(prompt('Sueldo: '))
    sueldo = sueldos.push()
}